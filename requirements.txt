# Vision Lens (AI资讯透视镜) — Dependencies
# Core
streamlit>=1.32.0
crewai>=0.30.0
pydantic>=2.6.0

# LLM — CrewAI handles LLM integration natively; anthropic SDK is pulled in as a dependency
anthropic>=0.25.0

# Web Search
tavily-python>=0.3.0
ddgs>=9.0
requests>=2.28.0

# Packaging (for single-executable builds)
pyinstaller>=6.0.0

# UI Enhancements
plotly>=5.18.0

# Desktop packaging
pywebview>=5.0

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
